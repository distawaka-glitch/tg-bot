services:
  - type: web
    name: tg-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    healthCheckPath: /
    envVars:
      - key: BASE_URL
        value: https://tg-bot.onrender.com
      - key: OWNER_ID
        value: "0"
      - key: TZ
        value: Europe/Moscow